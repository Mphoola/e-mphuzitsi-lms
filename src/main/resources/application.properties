spring.application.name=e-empuzitsi

# Database connection
spring.datasource.url=jdbc:mysql://localhost:3306/e_mphuzitsi_lms?useSSL=false&serverTimezone=UTC
spring.datasource.username=root
spring.datasource.password=root
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA / Hibernate settings
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.database-platform=org.hibernate.dialect.MySQLDialect

spring.jpa.open-in-view=false

# JWT Configuration
# JWT Configuration
app.jwt.secret=mySecretKeyThatMustBeAtLeast256BitsLongForHMACAlgorithmToWorkProperlyInJWTTokens
app.jwt.expiration=86400

# Email Configuration - Flexible for any provider
# Default: Mailtrap (for testing)
spring.mail.host=sandbox.smtp.mailtrap.io
spring.mail.port=2525
spring.mail.username=a8c01643c7f0ed
spring.mail.password=b665d6a3608359
spring.mail.protocol=smtp



# SMTP Properties (works with most providers)
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.starttls.required=false
spring.mail.properties.mail.smtp.ssl.enable=false
spring.mail.properties.mail.smtp.connectiontimeout=10000
spring.mail.properties.mail.smtp.timeout=10000
spring.mail.properties.mail.smtp.writetimeout=10000

# Application Email Settings
app.email.from=noreply@e-empuzitsi.com
app.email.name="E-Empuzitsi LMS"
app.email.reply-to=support@e-empuzitsi.com
app.email.frontend-url=http://localhost:3000

# Security Configuration

# Security Configuration
logging.level.com.mphoola.e_empuzitsi.security=DEBUG
logging.level.org.springframework.security=DEBUG
logging.level.com.mphoola.e_empuzitsi=DEBUG
logging.level.org.springframework.web=DEBUG
logging.level.org.hibernate.SQL=DEBUG
logging.level.org.hibernate.type.descriptor.sql.BasicBinder=TRACE

# Show detailed error messages
server.error.include-message=always
server.error.include-binding-errors=always
server.error.include-stacktrace=on-param
server.error.include-exception=true

# Handle 404 errors properly for API (Spring Boot 3.x approach)
spring.web.resources.add-mappings=false

# Jackson Configuration - Fix LocalDateTime serialization
spring.jackson.serialization.write-dates-as-timestamps=false
spring.jackson.default-property-inclusion=NON_NULL
spring.jackson.time-zone=UTC