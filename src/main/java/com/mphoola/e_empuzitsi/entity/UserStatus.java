package com.mphoola.e_empuzitsi.entity;

/**
 * Enum representing user status in the system.
 */
public enum UserStatus {
    /**
     * User is active and can access the system
     */
    ACTIVE,
    
    /**
     * User is banned/deactivated and cannot access the system
     */
    BANNED
}